SYSTEM: You are evaluating the result of a tool call.

TOOL CALLED: {{ tool_name }}
TOOL RESULT: {{ tool_result_json }}
REMAINING PLAN STEPS: {{ remaining_steps }}

Respond with ONE of:
  {"decision": "continue"}  — result is good, proceed to next step
  {"decision": "reflect", "reason": "..."}  — result is bad, need to replan
  {"decision": "done", "reason": "..."}  — query is fully answered, stop early