# Optional provider adapters for LangChain-backed LLM calls.
#
# These are not required for the core upload, analysis, auth, dashboard, or
# streaming demo. Install them only if you need the LangChain provider path and
# are prepared to align their numpy constraints with your local ML stack.
langchain>=0.1.10,<1
langchain-anthropic>=0.1.10,<1
langchain-openai>=0.1.0,<1
langchain-google-genai>=1.0.0,<3
langchain-groq>=0.1.0,<1
